---
description: Less 样式规范
globs:
  - "**/*.less"
alwaysApply: false
---

# Less 样式规范

## 文件职责

| 文件 | 用途 |
| --- | --- |
| `src/styles/tokens.less` | 间距、圆角、字体、阴影、z-index 等尺寸 token |
| `src/styles/variables.less` | 中性色 CSS 变量、品牌粉色（import tokens.less） |
| `src/styles/mixins.less` | 通用混合器（`.text-md()`、`.shadow-hover()` 等） |
| `src/styles/global.less` | 全局重置 |
| `src/components/*/index.less` | 组件私有样式 |

完整规范见 `docs/guide/design-token.md`。

## 写法

```less
@import '@styles/variables.less';
@import '@styles/mixins.less';

@btn-prefix: ~'@{prefix}-btn';

.@{btn-prefix} {
  .flex-center();
  .text-md();
  color: @text-primary;
  background: @bg-card;
  border: 1px solid @border-base;
  border-radius: @radius-sm;
}
```

## 颜色使用规则

### 中性色（文字 / 背景 / 边框）

- 正文、标题：`@text-primary`
- 描述、辅助：`@text-secondary`
- 占位符：`@text-placeholder`
- 页面底色：`@bg-page`
- 卡片底色：`@bg-card`
- 次要背景：`@bg-weak`
- 悬浮背景：`@bg-hover`
- 边框 / 分割线：`@border-base`

### 品牌粉（仅强调 / 交互）

- 主按钮、高亮、激活态：`@color-primary`
- hover / 按下：`@color-primary-hover`
- 淡粉背景（标签、text 按钮 hover）：`@color-primary-bg`
- 固定粉色阶梯（渐变、focus 环等）：`@jay-pink-base`、`@jay-pink-1` ~ `@jay-pink-8`

**禁止**将粉色用于正文文字或页面底色。粉按钮内文字固定 `#fff`。

`fade()` 等 Less 颜色函数不能作用于 CSS 变量别名，半透明效果请用静态 `@jay-pink-base` 或 `@jay-pink-2`。

## 尺寸 Token 使用规则

- 间距：`@spacing-xs` ~ `@spacing-xxl`（4px 栅格）
- 圆角：小控件 `@radius-sm`，默认卡片 `@radius-lg`，弹窗 `@radius-xl`，胶囊 `@radius-round`
- 字体：正文 `@font-md` + `@line-height-md`，或 mixin `.text-md()`
- 阴影：静态 `@shadow-sm`，hover `@shadow-md` / `@shadow-lg`
- 层级：内容 `@z-content`，流光 border `@z-card-hover`，下拉 `@z-dropdown`，Modal `@z-modal`

## 其他规则

- 组件样式禁止写全局选择器（`body`、`html` 等）
- **禁止写死** 4 / 8 / 12 / 16 等 magic number，必须引用 token
- 使用 BEM + `@prefix` 前缀，避免样式污染
- 动画名使用静态字符串（如 `jerry-btn-spin`），避免 `@{var}-name` 插值
