/** @deprecated 请从包入口或 `@types` 导入主题常量 */
export {
  JayPink,
  LightTheme,
  DarkTheme,
  JAY_PINK,
  JAY_PINK_DARK,
  JAY_PINK_LIGHT,
  PRIMARY_COLOR,
  THEME_COLORS,
} from '@types';

export type { ThemeMode, ThemeConfig, NeutralColorConfig, BrandColorConfig } from '@types';
